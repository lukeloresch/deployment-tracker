# deployment-tracker


Requirements: 
need to have in db:
  - service name
  - branch deployed from
  - commit ID Tag
  - service.json file
  - environment

need to pass in all the same things in at command line
